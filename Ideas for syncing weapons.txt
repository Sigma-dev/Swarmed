Spawning:
    Plan A:
        Check if local, if it is spawn it as a child of a camera else spawn it on an empty transform (to avoid duplicate cameras) V

Sync animations:
    Plan A:
        Sender:
            create a NetworkedWeapon Component that is listening to the weapon events and sends NetworkObjectData packet with inside, the network identity of the player controller, an action id of 0 and in the data a single u8 that maps to an action
        Receiver:
            change bevy_p2p so that it sends events when receiving an ObjectData packet, with the data inside the event.
            then NetworkedWeapon reads that message, checks if the network_identity fits with it's parent's, then if action id is 0 then it plays the anim from the index (make an enum :D)
        PROBLEM:
            Once the receiver receives the packet and sends a new weapon_event, then it will act as a sender for it's own event, triggering an infinite loop
Sync damage
    Plan A:
        Sender:




Ghost glock debugging:

What it's not:
The correct scene is spawned and remains existing.
It is not a visibility issue
Global position of the animated gltf is follows networked player

What it is:

    